{
  "name": "booker-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.10.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node index.js",
    "build:ui": "npm install && rm -rf build && cd ../frontend && npm run build && cp -r build ../backend",
    "deploy": "git push heroku master",
    "deploy:full": "build:ui && git add . && git commit -m \"Deploying to Heroku\" && git push heroku master"
  },
  "author": "",
  "license": "ISC"
}
